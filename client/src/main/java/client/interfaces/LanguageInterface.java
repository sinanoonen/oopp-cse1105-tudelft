package client.interfaces;

import client.utils.Language;

/**
 * Interface which ensures that a scene can change languages.
 */
public interface LanguageInterface {
    public void changeLanguage(Language language);
}
